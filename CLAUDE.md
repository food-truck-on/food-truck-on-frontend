# food-truck-on

푸드트럭 사장님이 오늘의 위치·영업시간·메뉴를 등록하고, 손님이 링크나 지도로 확인하는 서비스.
React 웹을 네이티브 웹뷰로 감싼 앱. 장기 목표는 앱인토스 미니앱 입점.

## 이 문서를 읽는 규칙

- 아래 세 파일이 이 프로젝트의 규칙 전부다. 여기 없는 관례를 임의로 만들지 마라.
- 규칙과 충돌하는 코드를 발견하면 코드를 고쳐라. 규칙을 따라 새 예외를 만들지 마라.
- **`docs/product-rules.md`에 `[미정]`으로 표시된 항목은 구현하지 마라.** 그 부분에 닿는 작업이면 멈추고 무엇을 정해야 하는지 알려라.

@docs/design-system.md
@docs/frontend.md
@docs/product-rules.md

## 팀

- 디자인 + 프론트엔드: 1명
- 백엔드: 별도

## 명령어

```
npm run dev     # 로컬 개발 서버
npm run build   # 프로덕션 빌드
npm run lint    # 린트. 작업 완료 보고 전에 반드시 실행한다
```
