import { RoutePlaceholder } from "@/components/RoutePlaceholder/RoutePlaceholder";

export default function HomePage() {
  return (
    <RoutePlaceholder
      path="/"
      title="스플래시 → 역할 선택"
      note="사장님으로 시작하기 / 손님으로 둘러보기"
    />
  );
}
