# 프로젝트 노트

## 확정 사실

- 내 역할: 피그마(디자인) + 프론트엔드
- 앱 형태: 웹뷰
- 프론트: **React + Next.js (App Router) 확정 (9/9).** `/t/:slug`의 카톡 OG 미리보기 때문. 백엔드와는 무관한 결정
- 백엔드: Node.js. 백엔드 담당분이 API 명세서와 ERD를 작성해 주심
- 메뉴는 **영업일마다 스냅샷** — 백엔드 `SessionMenu` 테이블로 확인 완료
- 품절은 **당일만 유지**, 다음 날 새 세션이 생기면 자동 초기화

- 서버: **AWS EC2 위의 자체 Node.js 서버** (팀 확인). Supabase는 쓰지 않는다

> **Supabase는 후보에서 빠졌다 — 2026-09-08**
> 백엔드 레포 세 문서(`CLAUDE.md`·`ERD.md`·`API_SPEC.md`)를 grep한 결과 Supabase 언급이 **0건**이고,
> `POST /api/v1/auth/social`이 자체 JWT를 발급하는 구조다. EC2 자체 서버 방침과도 일치한다.
> 따라서 **소셜 로그인은 직접 구현**이고, 이미지 저장소만 확인하면 된다(S3 여부).
> ERD 문법은 MySQL 계열(`INT AUTO_INCREMENT`, `ENUM`, `DATETIME`)이지만 DB 이름은 명시돼 있지 않다. 아래 Delegate에서 확인할 것.

## 우선순위

**백엔드는 아직 API 작성 중이다(9/8). 그래서 내 다음 행동은 백엔드 답을 기다리지 않는 것부터 한다.**

① ~~렌더링 방식 결정~~ → **Next.js 확정 (9/9). 프로젝트 생성 완료**
→ ② 토큰 완료 → **공통 컴포넌트 5개가 지금 할 일**
→ ③ 목데이터로 화면 퍼블리싱 + 카카오맵
→ ④ 실제 API 연동 — 백엔드 완료 후
→ ⑤ OG 미리보기 포함 공유 링크 페이지

**백엔드에 던져놓고 답을 기다리는 것** (①~③과 병행)
- 품절 토글 엔드포인트 요청 (없으면 `09 품절 처리` 화면을 못 붙인다)
- 알림 1차 범위 제외 통보 — 늦을수록 백엔드 작업이 낭비된다
- `ERD.FoodTruck` 로고·업종 컬럼, `MenuItem.display_order` 추가
- 손님 계정 개념 한 줄 합의 (백엔드는 이미 기기 토큰으로 만들어 둠)

---

# 할 일

## Delegate — 남에게 넘길 것 (먼저 던져놓고 답 기다리는 동안 Do 진행)

### 인프라 · 스택

- [ ] ~~Supabase와 EC2를 둘 다 쓰나요?~~ → **EC2 자체 서버로 확정 (9/8).** Supabase 관련 질문 전부 소멸
- [ ] ~~카카오 로그인을 Supabase Auth로 붙이나요?~~ → **직접 구현으로 확정.** `POST /api/v1/auth/social`이 자체 JWT 발급
- [ ] ~~API를 프론트가 직접 호출하나요?~~ → **명세에 답이 있음.** 20개 엔드포인트가 전부 `/api/v1` 아래
- [ ] **DB는 RDS MySQL인가요?** ERD 문법은 MySQL 계열인데 문서에 명시가 없습니다
- [ ] **트럭 로고 이미지는 S3에 올리나요?** 업로드 엔드포인트가 필요한지 알아야 합니다
      **그 전에 — `ERD.FoodTruck`에 로고·업종 컬럼이 아예 없습니다.** 추가해 주셔야 합니다
- [ ] 웹뷰 껍데기에서 푸시 알림을 받을 수 있는 구조인가요? (1차 범위는 아니지만 껍데기 구조 문제라 미리)

### API 수정 요청 (2026-09-08 명세 검토) — **9/8 백엔드에 전달 완료. 답변 대기**

- [ ] **세션 메뉴 품절 토글 엔드포인트를 만들어 주세요.**
      현재 `PATCH /food-trucks/:id/menus/:menuId` 하나뿐인데 이건 트럭 마스터를 영구 수정합니다.
      품절은 당일만 유지돼야 하므로 `SessionMenu`를 대상으로 하는 엔드포인트가 필요합니다.
      예: `PATCH /api/v1/sessions/:sessionId/menus/:sessionMenuId` (body: `is_sold_out`)
      **이게 없으면 디자인 `09 품절 처리` 화면을 API에 붙일 수 없습니다.**
- [ ] **`5-1` 응답의 `menus`가 데이터가 아니라 화면 문구입니다.**
      `"menus": [{ "id": 1, "name": "타코야끼 8알 외 2개" }]` — "외 2개"는 홈 화면 요약 문구입니다.
      메뉴 배열을 그대로 주시면 "외 N개"는 프론트에서 조합하겠습니다.
- [ ] **`6-2`에 `lat`/`lng` 쿼리 파라미터가 없는데 응답에 `distance_m`이 있습니다.** 계산이 불가능합니다. (`6-1`에는 있습니다)
- [ ] **`MenuItem`에 정렬 순서 컬럼(`display_order`)이 없습니다.** 메뉴 순서가 보장되지 않습니다.
- [ ] **`User → FoodTruck`이 ERD는 1:N인데 `GET /food-trucks/me`는 단일 객체를 반환합니다.** 사장님당 트럭은 1개인가요?
- [ ] **`share_url` 표현을 통일해 주세요.**
      ERD는 `/tk-달인-오늘`, API 2-1은 `https://오늘어디서팔아요.kr/tk-달인-오늘`, API 6-2는 `:shareSlug`로 셋이 다릅니다.
      **슬러그만 저장·반환**해 주시면 프론트가 `/t/:slug`로 조립하겠습니다. 도메인이 바뀌어도 DB를 안 건드립니다.
      한글 슬러그는 카톡·문자에서 `%ED%83%80…`로 깨지므로 영문을 권합니다.
- [ ] **`/t/:slug`의 OG 태그(카카오톡 미리보기)를 누가 렌더링하나요?**
      현재 `6-2`는 JSON만 반환합니다. 링크 공유가 이 서비스의 핵심인데 미리보기가 없으면 아무도 안 누릅니다.

### 범위 합의

- [ ] **알림은 1차 범위에서 뺍니다.** UI 껍데기만 만들고 구독 API는 연결하지 않습니다.
      백엔드에 `NotificationSubscription` 테이블과 `7-1`·`7-2` API가 이미 있는데, **더 진행하지 말아 달라고 전달해야 합니다.**
- [ ] 손님 계정 개념 한 줄 합의 (손님탭에 로그인이 필요한가)
      백엔드는 이미 `device_token` 기반으로 만들어 뒀습니다(`ERD.md` 99행). 합의된 게 아니라 한쪽에서 정해진 상태입니다.
- [x] ~~마감 방식 합의~~ → **B(수동 마감)로 확정 (9/9).** 백엔드 추가 작업 없음. `POST /sessions/:sessionId/close` 그대로 쓴다
- [ ] 손님탭 구성: 초안 그려서 팀에 던지기 → 혼자 결정할 사안 아님

## Do — 지금 내가 할 것

### 결정 (앉아서 한 번에 끝내기)

- [ ] 지도는 카카오맵으로 확정 → 국내 예제 많고 `react-kakao-maps-sdk` 존재, 마커는 컴포넌트로 처리
- [x] 문서는 `CLAUDE.md`를 짧게 두고 `docs/`로 분리 → **완료 (2026-09-08).**
      실제 구성은 `docs/design-system.md`, `docs/frontend.md`, `docs/product-rules.md` 세 개.
      (`conventions.md`라는 이름은 쓰지 않았음. `frontend.md`가 그 역할)
- [x] 품절은 당일 하루만 적용으로 확정 → `SessionMenu` 스냅샷에만 기록. 자정 배치 만들지 않음
- [x] **마감 방식 결정** → **B(사장님이 직접 마감) 확정 (2026-09-09).**
      백엔드가 이미 `POST /sessions/:sessionId/close`만 구현해 둬서 **백엔드 추가 작업 0.**
      자동 종료는 만들지 않는다. 마감 시각은 안내 문구일 뿐 상태 판정의 근거가 아니다.
      **내가 할 일: 디자인 `홈 / 영업 중` 화면 1개 추가** (마감 버튼 자리). 상세는 `docs/product-rules.md`.

### 디자인

- [ ] 색상 변수·타이포·간격 토큰 (TDS 토큰 이름 규칙 따라가기)
- [ ] 기본 컴포넌트 (TDS 컴포넌트 단위 기준)
- [ ] OG 미리보기 포함 공유 링크 페이지
- [ ] 카카오/구글 페이지로 넘어갔다 돌아오는 사이의 로딩 화면
- [ ] 사용자가 동의 안 하고 취소했을 때 화면
- [ ] 로그인 실패했을 때 화면

### 프론트

- [x] 프로젝트 세팅, 폴더 구조 → **완료 (9/9).** Next.js 16 + TS + ESLint, `src/{app,components,features,styles,lib}`. `src/pages/`는 만들지 않는다(구형 라우터와 충돌)
- [x] 디자인 토큰을 코드로 옮기기 → **완료 (9/9).** `src/styles/tokens.css` 색 15 + 간격 7 + 타이포 6, 원시/의미 2단
- [ ] `api/` 폴더 하나에 목데이터 함수 몰아두기 → 나중에 이 파일 내부만 fetch로 교체
- [ ] 목데이터로 화면 퍼블리싱
- [ ] 카카오맵 + 마커 붙이기 (백엔드 답변과 무관하니 먼저 해볼 수 있음)
- [ ] 카카오/구글 로그인 → 3주차. 자체 서버 방식이라 `code`를 백엔드에 넘기는 구조로 짠다
      **구글은 웹뷰 안에서 OAuth를 차단**하므로 외부 브라우저로 띄워야 한다
- [ ] API 명세서 보고 응답 타입 정의
- [ ] 실제 API 연동

## Defer — 나중에

- [ ] 알림 서비스 실제 구현 → **UI 껍데기만 만든다. 구독 API는 연결하지 않는다.**
      버튼은 **비활성 상태**로 두거나 누르면 "준비 중이에요" 안내가 뜨게 한다. 눌러도 아무 일 없는 활성 버튼은 두지 않는다 (데모에서 누군가 반드시 누름).
      "알림을 쓸 거냐 말 거냐" yes/no는 미루지 말 것 (웹뷰 푸시는 껍데기 구조 문제라 늦게 바꾸면 비쌈)
- [ ] TDS 실제 이관
- [ ] 인앱토스 입점

## Delete — 안 하기로 함

- ~~별도 랜딩페이지~~ → 공유 링크 페이지가 그 역할을 함
- ~~네이버지도 검토~~ → 카카오맵으로 정리됨
- ~~프론트에서 품절 자정 해제 시간 계산~~ → 백엔드 필드로 대체
- ~~ERD 직접 활용~~ → 화면에 띄울 수 있는 정보인지 확인할 때만 참고, 작업 대상 아님

---

## 일정

- **마감: 9월 26일 (토)**
- 9월 18~19일 여행 — 작업 불가
- **회고 2회: 9/17(목) 중간, 9/26(토) 최종** — 팀 노션에 작성
- 개발일지는 매일 `docs/devlog/YYYY-MM-DD.md` — `/log` 명령으로 작성
- 오늘 9월 8일(화) 기준 **실제 작업 가능일 16일**

> **백엔드는 아직 API 작성 중이다 — 2026-09-08 확인**
> 그래서 이 계획은 **API 연동을 마지막에 몰아두고, 그 전까지는 목데이터로 화면을 완성**하는 순서로 짰다.
> 목데이터 함수를 `src/lib/mock/` 한 곳에 몰아두면 API가 늦어져도 화면 작업이 멈추지 않는다.

### 주차별 계획

| 주차 | 기간 | 목표 | 끝났을 때 상태 |
|---|---|---|---|
| **1주차** | 9/8(화)~9/13(일) | **기반 + 컴포넌트** | 프로젝트가 돌아가고, 토큰·공통 컴포넌트 5개가 코드와 피그마 양쪽에 존재 |
| **2주차** | 9/14(월)~9/20(일)<br>*18·19 여행* | **화면 퍼블리싱 (목데이터)** | 사장님 13화면 + 손님 2화면이 목데이터로 다 보임. 지도 붙음 |
| **3주차** | 9/21(월)~9/26(토) | **연동 + 마감** | 실제 API 연결, 공유 링크 페이지, 로그인, 배포 |

**주차별 상세**

- **1주차** — 렌더링 방식 결정 → 프로젝트 세팅 → `tokens.css` → 공통 컴포넌트 5개(`Button` `TextInput` `Toggle` `MenuCard` `BottomNav`) → 피그마에도 같은 이름으로 컴포넌트·변수 만들기.
  틈틈이 **없는 화면 3개**(트럭 프로필 등록 / `/t/:slug` 공유 페이지 / 오늘은 쉬어요) 디자인.
- **2주차** — 목데이터로 화면 전부 퍼블리싱. 로딩·에러·빈 상태를 화면마다 같이 만든다(나중에 몰아서 하면 반드시 빠진다).
  카카오맵 + `CustomOverlayMap` 커스텀 핀. 여행 전 16~17일에 지도를 끝내두면 여행 중 막혀도 손해가 적다.
  **17일에 중간 회고를 쓴다.** 작업을 어중간하게 끊고 여행을 가지 않도록, 17일은 마무리와 회고에 시간을 남겨둔다.
- **3주차** — API 명세 확정본으로 응답 타입 정의 → 목데이터 함수 내부만 `fetch`로 교체 → 소셜 로그인 → OG 태그 붙은 공유 링크 페이지 → 배포·QA.
  **21일까지 백엔드 API가 안 오면 목데이터 버전으로 제출하는 걸 기본값으로 둔다.** 그 판단을 21일에 한다.

### 회고 — 9/17, 9/26

**팀 노션에 작성한다.** 깃허브 `docs/devlog/`의 일일 로그와는 다른 문서다.
일일 로그는 *오늘 무엇을 왜 했는지*의 사실 기록이고, 회고는 *방향이 맞았는지*를 묻는 자리다. 같은 내용을 옮겨 붙이지 마라.
회고를 쓸 때는 그 구간의 `docs/devlog/*.md`를 먼저 다 읽고 시작한다. 기억이 아니라 기록을 근거로 쓴다.

| 날짜 | 성격 | 다루는 구간 |
|---|---|---|
| **9/17 (목)** | 중간 회고 · **여행 전 마지막 작업일** | 9/8~9/17 (1주차 + 2주차 전반) |
| **9/26 (토)** | 최종 회고 · 마감일 | 프로젝트 전체 |

**9/17 중간 회고 — 답해야 할 것**

- 1주차 목표(토큰 + 공통 컴포넌트 5개)를 실제로 끝냈나. 못 끝냈으면 무엇이 시간을 먹었나
- 남은 화면 수 × 남은 작업일로 **26일이 가능한 숫자인가.** 안 되면 여기서 범위를 줄인다
- 백엔드 API가 언제 오는지 — 3주차 연동 일정이 이 답에 걸려 있다
- **여행 이틀(18·19) 동안 멈춰도 괜찮은 상태인가.** 진행 중인 작업을 어중간하게 두고 가면 복귀에 하루를 쓴다

> **이 회고는 마감 9일 전에 범위를 줄일 수 있는 마지막 기회다.** 21일에 깨달으면 이미 늦다.

**9/26 최종 회고 — 답해야 할 것**

- 세운 계획과 실제가 어디서 갈라졌나. 왜 갈라졌나
- 처음 해본 디자인 → 프론트 과정에서 **다음에 순서를 바꿀 것**은 무엇인가
  (예: 토큰·컴포넌트를 먼저 만든 판단이 옳았나, 화면부터 그렸어야 했나)
- 백엔드와의 협업에서 **더 일찍 물었어야 했던 것**은 무엇인가
- 남은 `[미정]`과 미구현 항목 정리 — 다음 단계(TDS 이관, 앱인토스 입점)로 넘길 목록

### 9월 11일(금)까지 — 상세

**9/8 (화) · 오늘**

- [x] ~~렌더링 방식 결정~~ → **Next.js 확정 (9/9)**
- [x] ~~마감 방식 결정~~ → **B 확정 (9/9)**
- [ ] 백엔드에 명세 검토 결과 전달 (아래 Delegate 목록)
      급한 3개를 앞에 둘 것: ①알림 중단 ②품절 엔드포인트 ③ERD 컬럼 3개 추가
- [x] 프로젝트 생성 → **완료 (9/9).** 폴더는 `src/{app,components,features,styles,lib}`
- [x] 라우트 5개 뚫기 → **완료 (9/9).** 5개 모두 200 확인

**9/9 (수)**

- [x] `src/styles/tokens.css` — **완료 (9/9)**
- [x] 주아체 웹폰트 로드 + `font-weight: bold` 리셋 → **완료 (9/9).**
      `next/font/google`의 `Jua`. **`subsets`를 지정하면 안 된다** — next/font의 폰트 목록에 주아체의
      korean 서브셋이 빠져 있어서 `subsets: ["latin"]`으로 두면 한글 글리프를 안 받아온다.
      생략 + `preload: false`로 두면 구글이 주는 CSS 전체(한글 포함)를 쓴다. 빌드 결과에서 `AC00` 범위 확인함
- [x] 웹뷰 기본 대응 → **완료 (9/9).** 안전영역은 `--safe-*` 토큰 + `.app-shell` 한 곳에서만 처리. `viewportFit: "cover"` 필수(없으면 `env()`가 0)
- [x] 360px 기준 레이아웃 → **완료 (9/9).** `.app-shell` (flex, `max-width` 480px, 고정 폭 없음)
- [ ] **피그마에 색상 변수 15개 생성** — 현재 피그마는 변수 0개, 하드코딩 색 31종 상태다

**9/10 (목)**

- [ ] `Button` — variant 4(`primary` `secondary` `kakao` `google`) × state 4
- [ ] `TextInput` — `default` `focus` `error`. 포커스는 `--border-focus` 2px, 라벨 필수
- [ ] `Toggle` — `on` `off` `disabled`
- [ ] 세 컴포넌트를 한 화면에 모아 보는 확인용 페이지 만들기

**9/11 (금)**

- [ ] `MenuCard` — `available` `soldout`. **메뉴명 15자 이상으로 반드시 테스트** ("매콤 크림치즈 타코야끼 세트")
- [ ] `BottomNav` — `owner` `customer`. **손님용에 사장님 탭을 넣지 말 것** (현재 디자인 오류)
- [ ] 토스트 + 확인 다이얼로그 공통 컴포넌트
- [ ] **피그마에도 같은 이름·variant로 컴포넌트 5개 생성**
- [ ] `npm run lint` 통과 확인
- [ ] 백엔드 답변 도착분 반영 → `docs/`의 `[미정]` 정리

> **9/11 금요일 밤 기준 도달해야 할 지점**
> 프로젝트가 `npm run dev`로 돌아가고, 라우트 5개가 열리고, 토큰과 공통 컴포넌트 5개가 완성돼 있다.
> 여기까지 되면 2주차는 **화면을 조립만 하면 되는** 상태가 된다. 늦어지면 2주차 전체가 밀린다.

### 이번 주에 안 하는 것

- API 호출 코드 — 명세 확정 전까지 작성하지 않는다 (`docs/frontend.md` 규칙)
- 소셜 로그인 실제 연동 — 3주차
- 알림 — 1차 범위 밖. 비활성 UI만
- TDS 이관, 앱인토스 입점 — 마감 이후

---

## 부록: 용어 정리

- **목데이터** — 백엔드 API가 아직 없을 때 코드에 박아두는 가짜 데이터. `api/` 한 곳에 몰아두면 나중에 그 안쪽만 fetch로 바꾸면 됨.
- **랜딩페이지** — 서비스를 처음 보는 사람에게 무엇을 하는 서비스인지 보여주는 소개 페이지.
- **웹뷰** — 앱 껍데기 안에 브라우저 창을 띄워 웹을 보여주는 방식. React 웹을 만들면 그대로 앱 화면이 되고, 푸시·카메라 같은 네이티브 기능은 껍데기가 따로 연결해줘야 함.
- **API 명세서** — 어떤 주소로 무엇을 보내면 무엇이 오는지 적은 문서. 프론트가 매일 봄.
- **ERD** — DB에 어떤 데이터가 어떻게 저장되는지 그린 도표. 화면에 특정 정보를 띄울 수 있는지 판단할 때 참고.
- **소셜 로그인 흐름** — 프론트가 SDK로 인가 코드를 받아 백엔드에 넘기고, 토큰 교환은 백엔드가 함. 우리는 EC2 자체 서버라 백엔드가 `code`를 받아 자체 JWT를 돌려준다.
- **압정** — 지도 위 마커(marker). 카카오맵 React SDK에서 컴포넌트로 얹으면 됨.
- **TDS** — 토스 디자인 시스템. 토큰 이름과 컴포넌트 단위를 미리 맞춰두면 나중에 갈아타기 쉬움.
- **OG 미리보기** — 링크를 카톡 등에 붙였을 때 뜨는 썸네일·제목·설명. `<meta>` 태그로 지정.

---

## 부록: 프론트가 백엔드 API를 받는다는 게 무슨 뜻인가

> 처음 협업하면서 정리한 내용. 2026-09-08

### 왜 프론트 혼자서는 안 되나

화면은 내가 다 만들 수 있다. **데이터를 남길 수가 없어서** 백엔드가 필요하다.

사장님이 폰에서 "오늘 위치는 강남역"이라고 입력했다고 하자. 이 값이 사장님 폰 안에만 있으면
**손님 폰에서는 보이지 않는다.** 브라우저는 자기 기기 안의 저장소만 볼 수 있기 때문이다.
사장님이 앱을 지우거나 폰을 바꾸면 그 값도 사라진다.

그래서 데이터는 **모두가 접근할 수 있는 한 곳(서버)** 에 올려야 한다.
백엔드가 그 서버이고, **API는 그 서버에 말을 거는 창구**다.

| | 프론트(내 영역) | 백엔드 |
|---|---|---|
| 하는 일 | 화면을 그린다, 사용자 입력을 받는다 | 데이터를 저장한다, 꺼내준다, 권한을 검사한다 |
| 갖고 있는 것 | 지금 이 화면 | 전체 데이터 |

### API를 "받는다"는 건 구체적으로 뭔가

거창한 파일을 받는 게 아니다. **약속된 주소 목록**을 받는 것이다.

```
GET /api/v1/food-trucks/me     ← 이 주소로 물어보면
                               → 내 트럭 정보를 JSON으로 돌려준다
```

프론트가 하는 일은 네 단계뿐이다.

1. 그 주소로 요청을 보낸다 (`fetch`)
2. 돌아온 JSON을 받는다
3. 화면 상태에 넣는다
4. 그 상태로 컴포넌트를 그린다

**우리 프로젝트에는 이런 주소가 20개 있다.** 전부 `/api/v1` 아래에 있고,
사장님용 주소는 요청 헤더에 로그인 토큰(`Authorization: Bearer ...`)을 같이 보내야 한다.
토큰이 없으면 서버가 "당신 누구냐"며 거절한다. 손님용 주소는 토큰이 필요 없다.

### 명세서가 왜 코드보다 먼저 필요한가

**응답의 모양이 컴포넌트의 모양을 결정하기 때문이다.**

`MenuCard`를 만들려면 메뉴 하나가 어떤 값들로 이뤄지는지 알아야 한다.
`{ name, price, is_sold_out }`인지 `{ menu_name, cost, soldout }`인지에 따라 코드가 달라진다.
추측해서 만들어놓고 나중에 다르면 **컴포넌트를 쓰는 화면 전부를 고쳐야 한다.**

`docs/frontend.md`에 "확정 전에는 API 호출 코드를 작성하지 마라"고 적어둔 게 이 이유다.

### 그런데 왜 지금 목데이터로 화면을 만들 수 있나

**모양만 맞으면 출처는 상관없기 때문이다.**

```
지금:   화면 ← mock/trucks.ts (내가 손으로 쓴 가짜 JSON)
나중:   화면 ← fetch('/api/v1/...') (진짜 서버)
```

화면 입장에서는 둘 다 그냥 "객체 하나"다. 그래서 **호출부를 `src/lib/mock/` 한 곳에 몰아두면**
나중에 그 파일 내부만 `fetch`로 바꾸고 화면은 한 줄도 안 건드려도 된다.
반대로 화면 곳곳에 데이터를 흩뿌려 놓으면 연동할 때 전부 뒤져야 한다. **이 구조가 3주차 일정을 지킨다.**

목데이터를 만들 때는 **명세서의 필드 이름을 그대로 베낀다.** 그래야 나중에 그대로 맞물린다.

### ERD는 왜 프론트가 보나

**"이 정보를 화면에 띄울 수 있는가"의 답이 거기 있기 때문이다.**

DB에 컬럼이 없으면 API가 그 값을 줄 방법이 없고, 그러면 화면에 띄울 수도 없다.
실제로 우리는 `ERD.FoodTruck`에 **로고·업종 컬럼이 없다는 걸 발견**했다.
손님 화면 디자인에는 로고가 들어가 있는데 저장할 곳이 없었던 것이다.
디자인을 다 그린 뒤에 알았으면 화면을 다시 그려야 했다. **그래서 ERD를 먼저 본다.**

### 명세서를 검토하는 것도 프론트 일이다

받은 대로 쓰는 게 아니라 **화면과 대조해서 어긋난 곳을 찾아내는 것**까지가 프론트 일이다.
백엔드는 화면을 안 보고 쓰기 때문에 어긋나는 게 정상이다. 실제로 찾은 것들:

- `5-1`의 `"menus": [{ "name": "타코야끼 8알 외 2개" }]`
  → **"외 2개"는 데이터가 아니라 화면 문구다.** 데이터에 UI 문구가 섞이면 다른 화면에서 재사용을 못 한다.
  메뉴 배열을 받아서 "외 N개"는 프론트가 조합해야 한다.
- `6-2`는 응답에 거리(`distance_m`)를 주는데 **내 위치를 받는 파라미터가 없다.** 계산이 불가능하다.
- `MenuItem`에 정렬 순서 컬럼이 없다 → **메뉴 순서가 새로고침할 때마다 바뀔 수 있다.**

이런 건 코드를 쓰기 전에 잡아야 싸다. 만들고 나서 고치면 양쪽 다 다시 짠다.
